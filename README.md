# CIFAR10-CNN-Model
CNN Model used to classify the CIFAR10 dataset utilizing Keras' TensorFlow libraries
Utilizes several ML related techniques including standardization and one-hot encoding
Utilizes TensorFlow's Sequential model and related methods
Outputs the F1 score for the related model's classification predictions
